# k8s/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: knn-service
  namespace: threekov-lab7
spec:
  type: ClusterIP 
  selector:
    app: knn-service
  ports:
    - name: http
      port: 80
      targetPort: 8000
      protocol: TCP
