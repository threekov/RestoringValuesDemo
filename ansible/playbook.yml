- hosts: knn_servers
  become: yes
  
  tasks:
    - name: Update apt cache
      apt:
        update_cache: yes
        cache_valid_time: 3600

    - name: Install prerequisites
      apt:
        name:
          - apt-transport-https
          - ca-certificates
          - curl
          - software-properties-common
          - gnupg
        state: present

    - name: Add Docker GPG key
      apt_key:
        url: https://download.docker.com/linux/ubuntu/gpg
        state: present

    - name: Add Docker repository
      apt_repository:
        repo: "deb [arch=amd64] https://download.docker.com/linux/ubuntu jammy stable"
        state: present
        update_cache: yes

    - name: Install Docker
      apt:
        name:
          - docker-ce
          - docker-ce-cli
          - containerd.io
          - docker-compose-plugin
        state: present

    - name: Ensure Docker is running
      systemd:
        name: docker
        state: started
        enabled: yes

    - name: Add user to docker group
      user:
        name: ubuntu
        groups: docker
        append: yes

    - name: Copy project files
      copy:
        src: ../
        dest: /opt/knn-service

    - name: Stop and remove existing container
      shell: |
        docker stop knn-service 2>/dev/null || true
        docker rm knn-service 2>/dev/null || true

    - name: Remove existing image
      shell: |
        docker rmi knn-service 2>/dev/null || true

    - name: Build Docker image
      shell: |
        cd /opt/knn-service
        docker build -t knn-service .

    - name: Run Docker container
      shell: |
        docker run -d -p 8000:8000 --name knn-service knn-service
